# Dependências / build
node_modules/
dist/
build/

# Sistema / editor
Thumbs.db
.DS_Store
*.log
*.tmp

# Config local (caminhos da máquina)
.claude/

# Material de origem — pesado e/ou com direitos de autor (não versionar)
Features/Manuais/*.pdf
Features/roby_assets 2.0/
Takova Design System-handoff.zip
Takova Design System-handoff/

# Temporários
scratchpad/

# Motor do Flutter descarregado a mao (ver android/build.gradle.kts):
# so existe em maquinas onde o Gradle nao consegue falar com o servidor.
somara-flutter/android/flutter-engine-repo/

# Skills de ferramentas externas (reinstalaveis: npx skills add higgsfield-ai/skills)
.agents/

# APK compilados — sao gerados a partir do codigo, nao se versionam.
# O historico das versoes vive nos commits e nas etiquetas do git.
APKs/
